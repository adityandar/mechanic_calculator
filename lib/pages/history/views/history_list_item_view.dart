import 'package:flutter/material.dart';

class HistoryListItemView extends StatelessWidget {
  const HistoryListItemView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
